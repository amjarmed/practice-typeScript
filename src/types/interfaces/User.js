"use strict";
/* interface User {
  id: number;
  name: string;
  email: string;
  isActive: boolean;
}

const createUser = (user: User) => {
  console.log(`${user.name} with email ${user.email} was created.`);
}

const newUser: User = {
  id: 1,
  name: "John Doe",
  email: "john.doe@example.com",
  isActive: true,
};

createUser(newUser);
 */ 
